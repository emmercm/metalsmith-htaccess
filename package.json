{
  "name": "metalsmith-htaccess",
  "version": "0.1.2",
  "description": "A Metalsmith plugin to create an htaccess configuration file.",
  "homepage": "https://github.com/emmercm/metalsmith-htaccess#readme",
  "bugs": {
    "url": "https://github.com/emmercm/metalsmith-htaccess/issues"
  },
  "license": "GPL-3.0-or-later",
  "author": "Christian Emmer",
  "files": [
    "/lib"
  ],
  "main": "./lib/index.js",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/emmercm/metalsmith-htaccess.git"
  },
  "scripts": {
    "lint": "eslint . --fix || true",
    "test": "jest --verbose --coverage && eslint .",
    "test:update": "jest --verbose --coverage --updateSnapshot",
    "test:watch": "jest --verbose --watch",
    "test:junit": "JEST_JUNIT_OUTPUT=reports/junit/results.xml jest --ci --runInBand --reporters=default --reporters=jest-junit"
  },
  "dependencies": {
    "deepmerge": "^3.2.0"
  },
  "devDependencies": {
    "assert-dir-equal": "1.1.0",
    "eslint": "6.1.0",
    "eslint-config-airbnb-base": "13.2.0",
    "eslint-plugin-import": "2.18.2",
    "jest": "24.8.0",
    "jest-junit": "7.0.0",
    "metalsmith": "2.3.0"
  }
}
